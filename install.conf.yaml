- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.bashrc: dot/bashrc
    ~/.git-prompt.sh: dot/git-prompt.sh
    ~/.profile: dot/profile
    ~/.tmux.conf: dot/tmux.conf
    ~/.vimrc: dot/vimrc
